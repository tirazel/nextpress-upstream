{#
/**
 * @file
 * Theme override for a local task link.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper element.
 * - is_active: Whether the task item is an active tab.
 * - link: A rendered link element.
 *
 * Note: This template renders the content for each task item in
 * menu-local-tasks.html.twig.
 *
 * @see template_preprocess_menu_local_task()
 */
#}

{% if link['#title'] matches '/View/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="View"><i class="fas fa-eye" aria-hidden="true"></i> View </a></li>
{% elseif link['#title'] matches '/Edit/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="Edit"><i class="fas fa-edit" aria-hidden="true"></i> Edit </a></li>
{% elseif link['#title'] matches '/Delete/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="Delete"><i class="fas fa-trash" aria-hidden="true"></i> Delete </a></li>
{% elseif link['#title'] matches '/Devel/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="Devel"><i class="fas fa-cog" aria-hidden="true"></i> Devel </a></li>
{% elseif link['#title'] matches '/Revisions/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="Revisions"><i class="fas fa-history" aria-hidden="true"></i> Revisions </a></li>
{% elseif link['#title'] matches '/Shortcuts/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="Favorite"><i class="fas fa-star" aria-hidden="true"></i> Shortcuts </a></li>
{% elseif link['#title'] matches '/Clone/' %}
    <li{{ attributes }}><a href="{{ link['#url'] }}" aria-label="Clone"><i class="fas fa-clone" aria-hidden="true"></i> Clone </a></li>
{% else %}
    <li{{ attributes }}>{{ link }}</li>
{% endif %}
